generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Painel {
  id        String   @id
  user_id   String
  name      String
  value     Float
  createdAt DateTime @default(now())
}
